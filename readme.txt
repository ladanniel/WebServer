本版本改动：
支持重定向操作。

重定向对应的状态代码常用为302，并且要包含一个响应头：Location
值为要求浏览器再次发起连接时请求的路径
即：希望重定向的路径
重定向的响应中通常不包括响应正文。
对此，我们对HttpResponse进行重构，添加重定向功能。


1：在HttpResponse中添加一个重定向方法：
   void sendRedirect(String url)
   在方法中我们进行重定向相关设置操作。
   
 2：在注册和登录的Servlet中，在执行完业务后改为使用
    重定向方式跳转到提示页面。